# UI 重构进度

## 2026-08-05 历史知识提示词质量优化开工回执

- 目标：统一普通主题、“最”系列、朝代四件套的“史实正确＋真实有趣”编辑契约，不改 JSON 结构或前端。
- 顺序：锁定三模式质量测试并取红灯 → 抽取共享编辑规则 → 聚焦测试与类型检查 → 范围审计。
- 最大风险：两个 history-agent 文件已有未提交的风云人物修正，必须逐块追加并完整保留。
- 基线：`npm test -- agents/history-agent/src/index.test.ts` 为 1 文件、29/29 测试通过、跳过 0。
- 边界：提示词只能降低幻觉和过度断言，不能替代外部史料检索或保证绝对正确。
- 反向验证：新增三模式契约后先得到 3 failed / 29 passed；共享规则接入后为 32/32 通过、跳过 0。
- 实现：普通主题、“最”系列、朝代四件套及三条重试路径共用史实层级、比较口径、真实趣味、视觉与发布数据边界。
- 验收：`npm run typecheck` 与 `git diff --check` 通过；JSON 结构、依赖、前端和模型运行时未改。
- 范围说明：开工前已存在的两个 apps 测试文件仍在全局 diff 中，本轮未触碰；本轮新增修改仅在任务白名单。

## 首页模块尺寸目标开工回执

- 目标：8 个现存首页模块分别适配 5 档展开尺寸，共 40 个独立内容契约，并保留共享折叠态。
- 顺序：尺寸契约 → 会话/待办 → 热点/记账 → 梦幻西游/总结 → 历史/浏览器自动化 → 160 组合审计。
- 最大风险：`dashboard-page.tsx` 集中承载全部模块，旧 CSS 依赖外框裁切；必须逐模块改内容层级，不能只补覆盖样式。
- 2026-07-30 复核：工作区 84 条既有改动；白名单实现文件已有用户改动，全部保留。
- 基线：68/68 测试文件、421/421 测试通过，跳过 0；类型检查与 Web 构建通过。
- 构建基线：主 JS 1,143.12 kB（gzip 356.55 kB），低于 1.20 MB。
- 范围：8 个模块、5 档尺寸、40 个展开组合；共享折叠态保持不变。

## 2026-07-30 首页模块尺寸重构

- 尺寸契约完成：共覆盖 8×5=40 条显式规则。反向验证临时删除 `news:small` 后，同一命令真实得到 `1 failed / 2 failed | 1 passed`，关键断言为 `Expected 40 / Received 39`；恢复规则后得到 `1 passed / 3 passed`。
- 展示层完成：热点、会话、梦幻西游、待办、记账、历史、总结、浏览器自动化均按尺寸主动增减信息，不再依赖外框裁切。
- 最小档统一为“身份/状态/主操作”；较小档最多 3 条关键信息；中等档使用横向信息带；大档与最大档逐级增加明细和内部滚动。
- 共享外壳增加模块、尺寸、变体、折叠态和主操作审计标记；原尺寸几何、拖动、折叠、路由与存储键均保留。
- 浏览器自动化首页模块已接入现有 dashboard 数据；未新增 API、依赖、网络调用、遥测或权限。
- 视觉修正闭环：中等热点窄列、最小会话操作碰撞、历史中等裁切、梦幻西游/记账移动中等裁切，以及聊天/浏览器自动化中等档内容溢出，均经真实截图发现并修复。
- 严格视觉审计：桌面/移动 × 日间/夜间共 160/160 通过，失败 0；逐项验证固定几何、模块身份、稳定主操作、根节点与内容溢出，移动端主操作触控目标达到 44px。
- 折叠态审计：8 模块 × 桌面/移动 × 日间/夜间共 32/32 通过；折叠后只渲染身份、摘要和“展开”操作，尺寸与触控目标正确。
- 视觉证据：四张 40 格联系表与结构化 JSON 已写入 `apps/web/src/ui-audit/`。
- 最终回归：首次全量运行有 1 个未修改页面的剪贴板异步测试波动，单文件复跑 9/9 通过；随后全量复跑 69/69 测试文件、424/424 测试通过，跳过 0；类型检查、生产构建、`git diff --check` 均通过。
- 最终构建：主 JS 1,155.95 kB（gzip 359.53 kB），低于 1.20 MB 上限。
- 2026-07-30 领导视觉抽查已确认“视觉通过”；40 个展开组合与共享折叠态均获认可。
- 当前阶段：首页模块尺寸重构的实现、机器验收与视觉验收全部完成。

- 目标：把 17 个现有路由统一为克制、现代、信息清晰的生产力工具，同时保留全部业务行为。
- 顺序：共享 token/壳层 → 首页与导航 → 4 个溢出页 → 其余路由 → 全量回归与双视口巡检。
- 最大风险：`styles.css` 已有 14,970 行且层叠复杂，新增规则必须集中、可追踪，不能覆盖用户现有业务改动。
- 2026-07-30 基线：类型检查、Web 构建通过；主 JS 1,142.01 kB。
- 测试基线：67 文件/418 测试；本轮全量复核有 1 个数据同步测试首次超时，单独复跑 2/2 通过。
- 现有 Web 测试内容摘要：`a418bc64c69ca1774ac83b2313f4b9facd80ff61`。
- 开工前工作区状态摘要：`7fa3e7c59da4d0f7859f264d2477c2d9dc9527e3`；已有改动全部视为用户资产。
- 视觉原则：低装饰、单一青绿强调色、清晰层级、44px 移动触控目标、页面根节点不横向溢出。
- 当前阶段：根据领导视觉反馈重做全局色调并复核全部路由。

## 2026-07-30 实施记录

- 任务 1 完成：新增 `ui-contract.ts` 与 `ui-foundation.css`，统一路由、视口、触控、token、焦点态、表面层级和减弱动效。
- 任务 1 反向验证：契约测试先因缺少模块真实失败；实现后主题、首页布局与契约测试 26/26 通过。
- 任务 2 完成：移动导航默认只显示当前页，可展开全部可见入口；当前页不再受“隐藏导航入口”偏好影响。
- 首页模块拖动区由重复标题改为可聚焦“拖动”把手，保留排序、折叠、尺寸与存储行为。
- 390×844 首轮巡检：17/17 路由根节点 `scrollWidth=390`，原先溢出的 `/news`、`/history`、`/todo`、`/logs` 均已修复。
- 1280×720 首轮巡检：已加载页面根节点均无横向溢出；需等待数据页稳定后完成截图证据。
- 当前构建：主 JS 1,142.40 kB，低于 1.20 MB 上限。

## 2026-07-30 最终机器验收

- 全量测试：68/68 文件、420/420 测试通过，跳过 0；数据同步测试本轮稳定通过。
- 类型检查：`npm run typecheck` 通过。
- 生产构建：`npm run build:web` 通过；主 JS 1,142.65 kB，低于 1.20 MB。
- 补丁检查：`git diff --check` 通过。
- 原有 Web 测试摘要仍为 `a418bc64c69ca1774ac83b2313f4b9facd80ff61`，证明判卷文件未被本轮修改。
- 路由审计：34/34 视口组合均有当前导航、首屏主标题和主操作，根节点横向溢出、标题裁切、控件裁切均为 0。
- 移动端主操作均达到至少 44px；工具子页“返回上级”已由 40px 统一为 44px，工具目录整卡入口也纳入审计。
- 日间/夜间主题均在桌面和移动端验证；结束时恢复夜间主题。
- 视觉证据：`apps/web/src/ui-audit/mobile-contact-sheet.png`、`desktop-contact-sheet.png` 与 `route-audit.json`。
- 领导首次视觉抽查指出 AI 热点时间轴重叠，并要求全项目色调参考 `https://aihot.virxact.com/` 重新设计。

## 2026-07-30 视觉反馈修正

- 视觉主张：参考 AI HOT 的石墨黑蓝画布、低对比抬升面、灰蓝细边和青蓝强调；暖金仅用于精选/提醒。
- 内容结构：保留生产力工作区，不增加营销式 Hero；导航、主工作区、辅助信息三层分离。
- 交互语言：当前导航采用青蓝底与底边提示，列表/按钮仅做短促亮度和边界反馈，继续支持减弱动效。
- 已重建日/夜主题 token，并同步 Ant Design 的主色、容器色、文本色和边界色。
- AI 热点重叠根因：`news-digest--aihot` 声明 3 行网格，但“全部”视图存在 4 个直属子项，工具栏高度被压为 0，溢出的筛选项与时间轴叠加。
- 修复方式：增加 `news-digest__body` 内容层，使父级固定为 3 行、全部视图内部为“工具栏 + 时间轴”两行；未使用隐藏根溢出遮挡。
- 桌面实测：工具栏高 40px，底部 310.74；时间轴顶部 328.74，交叠为 false。
- 移动实测：工具栏高 154px，底部 505.04；时间轴顶部 523.04，日期/时间/标题交叠均为 false，根节点 `scrollWidth=390`。
- 17 路由夜间视觉巡检：桌面 17/17、移动 17/17 均无根节点横向溢出；联系表已按新色调重建。
- 日间主题巡检：桌面与移动共 34/34 组合均正确应用日间 token，根节点横向溢出为 0。
- AI 热点日间版补充抽查：时间轴结构清晰，标题与时间节点无交叠。
- 完整回归：68/68 文件、421/421 测试通过，跳过 0；类型检查、补丁检查通过。
- 生产构建：主 JS 1,142.86 kB（gzip 356.48 kB），低于 1.20 MB 上限。
- 当前阶段：实现与机器验收完成，等待领导视觉复核。

## 2026-07-30 第二轮视觉反馈

- 历史知识：移除旧暖金/深蓝渐变体系，主舞台、归档、数据卡片、模式切换和历史首页模块统一改用全局黑蓝表面与青蓝强调色。
- 梦幻西游交易：跨服、资产、物价三套独立色板统一映射到全局 token；库存、流水、弹层和表格取消蓝金装饰渐变。
- 管理页面：桌面改为“左侧配置概览 + 右侧配置工作区”，模型实例和子智能体由方形卡片墙改为横向可扫描配置行。
- 管理页面移动端：概览区按内容高度展开，随后依次呈现模型、子智能体与视觉配置，不再被长内容网格压缩。
- 实测：三页在 1280×720 与 390×844 夜间主题均无横向溢出；日间主题移动端三页也均无横向溢出。
- 色值核验：历史与梦幻西游夜间主表面均为 `rgba(16, 21, 28, 0.96)`，边界为 `rgba(255, 255, 255, 0.08)`，强调色为 `#4fa3b3`。
- 完整回归：68/68 文件、421/421 测试通过，跳过 0；类型检查与补丁检查通过。
- 生产构建：主 JS 1,143.12 kB（gzip 356.55 kB），低于 1.20 MB 上限。
- 当前阶段：第二轮视觉反馈与机器验收已完成，等待领导复核。
